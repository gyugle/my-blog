import Board from '../routes/Board';
import { data } from '../routes/Board';
function ShowContent() {
  return (
    <div>
      {console.log(data)}
      <h2></h2>
    </div>
  );
}
export default ShowContent;
